<h1>UI Kit Gallery</h1>
<p>Dependency-free AntDesign-style controls ported into WssBlazorControls. These are general UI
   widgets (not form Edit* controls). Icon / Button / Checkbox / Tag are intentionally not included.</p>

@* Composition of the per-component demos, each of which is also reachable on its own from the demo
   host's UI Kit sidebar. Sections of one component are contiguous here. Standalone="false" drops
   each demo's own <h1>: this page has one heading, and the extra headings shifted the committed
   visual baselines of the sections below them. *@
<div class="wss-demo-gallery">
    <DemoAlert Standalone="false" />
    <DemoSkeleton Standalone="false" />
    <DemoPopover Standalone="false" />
    <DemoPagination Standalone="false" />
    <DemoModalDrawer Standalone="false" />
    <DemoPopconfirm Standalone="false" />
    <DemoTable Standalone="false" />
    <DemoToasts Standalone="false" />
    <DemoTabs Standalone="false" />
    <DemoSearchInput Standalone="false" />
    <DemoSelectPill Standalone="false" />
    <DemoDateRangePicker Standalone="false" />
    <DemoDatePicker Standalone="false" />
    <DemoColorPicker Standalone="false" />
    <DemoTooltip Standalone="false" />
</div>
