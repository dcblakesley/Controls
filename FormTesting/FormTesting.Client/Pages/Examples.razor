@using Controls
@using Components
@using FormTesting.Client.Components

@page "/"

<h3>Minor Update</h3>

<div class="flex-row">
    <div class="flex-column">

    </div>
    <div class="flex-column">
        @switch (_view)
        {
            case "General":
                <General/>
                break;
            case "Bool":
                break;
            case "Date":
                break;
            case "Number":
                break;
            case "Radio":
                break;
            case "Select":
                break;
            case "TextArea":
                break;
            case "String":
                break;
        }
        <General/>
    </div>

</div>

@code {
    readonly Person _model = new();
    [Parameter]
    public bool IsEditMode { get; set; } = true;

    public FormOptions FormOptions { get; set; } = new ();

    readonly List<string> _colorOptions = ["", "Red", "Green", "Blue", "Yellow", "Orange", "Purple", "Black", "White"];
    List<Plant> _plants = Plant.GetTestData();

    List<string> _viewOptions = ["General", "Bool", "Date", "Number", "Radio", "Select", "TextArea", "String"];
    string _view = "General";
}
