@if (IsHidden)
{
    <div class="edit-control-wrapper @OuterClass">
        <FormLabel Id=@Id Label=@Label Description=@Description/>

        <div class="edit-control-readonly-text"
             role="textbox"
             id=@Id
             data-test-id=@Id
             aria-readonly="true"
             aria-labelledby=@($"lbl-{Id}")
             aria-describedby=@($"desc-{Id}")>	 

            @Text
        </div>
        
  </div>
}