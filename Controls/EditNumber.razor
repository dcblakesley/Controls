@typeparam T
@inherits EditTextControlBase<T>

@if (ShouldShowComponent())
{
    @* The consumer's `style` lands on the wrapper (one rule for every control -- see EditControlBase's
       unmatched-attribute remarks); the remaining unmatched attributes go on the <input> below. *@
    <div class="edit-control-wrapper @ContainerClass"
         style="@AttributeSplat.MergeStyle(null, AdditionalAttributes)">
        <FormLabel Id=@_id FieldIdentifier=@_fieldIdentifier Attributes=@_attributes Label=@Label LabelContent=@LabelContent IsRequired=@IsRequiredResolved Description=@Description Tooltip=@Tooltip IsLabelHidden=@ShouldHideLabel IsForLabelable=@ShowEditor />

        @if (ShowEditor)
        {
            @* ShowStepper wraps the SAME editor fragment in a button group -- rendered from one
               template rather than transcribed into both branches, so the default (no-stepper) branch
               provably emits today's markup byte for byte. *@
            @if (ShowStepper)
            {
                <div class="@StepperGroupClass">
                    <button type="button"
                            class="edit-number-step edit-number-step-down"
                            tabindex="-1"
                            disabled=@(IsDisabled || IsAtMin)
                            aria-label=@EffectiveDecreaseButtonLabel
                            @onclick="@(() => StepValue(-1))">
                        @EditIcons.Minus
                    </button>
                    @EditorFragment
                    <button type="button"
                            class="edit-number-step edit-number-step-up"
                            tabindex="-1"
                            disabled=@(IsDisabled || IsAtMax)
                            aria-label=@EffectiveIncreaseButtonLabel
                            @onclick="@(() => StepValue(1))">
                        @EditIcons.Plus
                    </button>
                </div>
            }
            else
            {
                @EditorFragment
            }
            @* Invisible sibling of whichever editor shape rendered above -- NEVER inside the stepper
               group, which is a locked three-slot flex row (down button, editor, up button). A closed
               <datalist> has no box per spec, so today a fourth child there would be invisible anyway;
               it takes exactly one consumer stylesheet or datalist polyfill that makes it displayable
               to turn that into a visible fourth flex item wedged into the group. Same reasoning as
               EditString.razor's placement remark: the datalist belongs beside the editor's layout, not
               inside it. Gated on SuggestionsListId so no dead markup renders when Suggestions is
               unset. *@
            @if (SuggestionsListId is not null)
            {
                <datalist id="@SuggestionsListId">
                    @foreach (var suggestion in Suggestions!)
                    {
                        <option value="@suggestion"></option>
                    }
                </datalist>
            }
        }
        else
        {
            <ReadOnlyValue Id=@_id CssClass=@CssClass Text=@GetFormattedNumber() AriaDescribedBy=@_describedBy />
        }

        <FieldValidationDisplay Id=@_id FieldIdentifier=@_fieldIdentifier Attributes=@_attributes Label=@Label />
        <BoundValueDisplay Text=@CurrentValueAsString />
    </div>
}

@code {
    // The editor itself, factored out of the markup above so ShowStepper's group wrapper and the
    // default branch render the exact same frames.
    //
    // @bind-value:event defaults to UpdateTrigger.Change (onchange), NOT Input (oninput): browsers
    // report type=number as "" while the text is a partial number ("-", "3.", "1e"), so per-keystroke
    // binding would flash "must be a number" mid-typing. Matches Microsoft's InputNumber. See
    // DefaultUpdateTrigger's doc comment in EditNumber.razor.cs for the full rationale (UpdateOn
    // itself now lives on EditTextControlBase<TValue>). (Comment deliberately outside the tag -- a
    // Razor comment inside an attribute list compiles into a literal attribute name and breaks
    // setAttribute.)
    //
    // Splat first so every explicit attribute below wins (inputmode/readonly/spellcheck/data-* are the
    // point; `class` travels CssClass and `style` the wrapper, so AttributeSplat.Rest withholds both).
    // A null splat emits no frames at all. The CONDITIONAL attributes -- `list` and the
    // disabled/aria-required/aria-invalid/aria-errormessage state set -- ride EditorAttributes (merged
    // in C#) rather than their own explicit attributes here; see EditorAttributes' and
    // EditControlBase.EditorStateAttributes' remarks: an explicit frame wins outright over an earlier
    // same-named one even when its own value is null, so writing them here would DELETE a consumer's
    // splatted value rather than merely decline to override it. What stays explicit below is
    // unconditional (`type="number"` is library-owned outright) or emitted by @bind-value.
    RenderFragment EditorFragment => @<EditInputShell IsInvalid=@IsInvalid
                                                      IsDisabled=@IsDisabled
                                                      WrapperClass=@(EditInputShell.SizeClass(Size))
                                                      Prefix=@Prefix
                                                      Suffix=@Suffix>
        <input @attributes="EditorAttributes"
               type="number"
               @ref="_editorRef"
               id=@_id
               data-test-id=@_id
               step=@EffectiveStep
               min=@(EffectiveMin?.ToString(CultureInfo.InvariantCulture))
               max=@(EffectiveMax?.ToString(CultureInfo.InvariantCulture))
               placeholder=@EffectivePlaceholder
               @bind-value=@CurrentValueAsString
               @bind-value:event="@UpdateEventName"
               class=@InputClass
               aria-labelledby="lbltext-@_id"
               aria-describedby=@_describedBy />
    </EditInputShell>;
}
